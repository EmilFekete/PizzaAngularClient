import { DiscountToolTipDirective } from './discount-tool-tip.directive';

describe('DiscountToolTipDirective', () => {
  it('should create an instance', () => {
    const directive = new DiscountToolTipDirective();
    expect(directive).toBeTruthy();
  });
});
